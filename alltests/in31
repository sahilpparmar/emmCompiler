int mul = 0;
int foo (int a) {
    while(true) {
        mul = mul * 2;
        a = a-1;
        if (a <= 0) break;
    }
    return mul;
}

event a(int aa);
a(ii) -> {
    mul = 1;
    mul = foo(ii);
    print ("2-power of param: ", mul);
}

